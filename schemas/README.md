# Schema Assets

This directory contains the machine-readable lesson specification and the strict JSON Schema validator.

Included files:
- `machine-readable-lesson-generation-spec.yaml`
- `lesson-generation.schema.json`

Use the YAML file as the conceptual planning model.
Use the JSON Schema as the structural validation model.
